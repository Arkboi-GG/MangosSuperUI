# ProcTriggeredData

<!-- documentation: model-written from source via the local LLM; review before trusting -->

# ProcTriggeredData

**Purpose & Responsibilities**

`ProcTriggeredData` is a lightweight aggregate struct defined in `Unit.h` within the `wowvmangos` codebase. Its sole responsibility is to bundle the context required to trigger a secondary spell effect (a "proc") resulting from a primary event, such as a spell hit or melee attack. It acts as a data carrier, grouping together the configuration of the proc (`SpellProcEventEntry`), the source of the trigger (`SpellAuraHolder`), the target of the effect (`Unit`), and the specific condition flags that were met (`procFlag`).

This struct is not a standalone entity with behavior; it has no methods other than its constructor. It exists exclusively to be stored in a `std::vector<ProcTriggeredData>` (aliased as `ProcTriggeredList`) and passed by reference or value to methods in the `Unit` class (specifically `Unit::HandleTriggers` and related proc handling logic) to ensure that all necessary information for resolving a chain of reactive spells is available in a single package.

**Member-by-Member Behavior**

The unit contains only one member: the constructor.

*   **`ProcTriggeredData`**: This constructor initializes the four data members of the struct. It takes four arguments:
    1.  `_spellProcEvent`: A pointer to a `SpellProcEventEntry`, which defines the rules and conditions under which the proc occurs (e.g., chance, cooldown, required flags).
    2.  `_triggeredByHolder`: A pointer to a `SpellAuraHolder`, representing the active aura on the unit that is responsible for triggering this proc.
    3.  `_target`: A pointer to a `Unit`, specifying the entity upon which the procced spell will be applied.
    4.  `_procFlag`: A `uint32` bitmask indicating the specific event type that triggered the proc (e.g., spell hit, melee damage, healing received).

    The constructor assigns these values directly to the corresponding public member variables. There is no validation, memory allocation, or side-effect logic in this constructor.

**Cross-Unit Boundaries**

According to the provided MAP, `ProcTriggeredData` has no outgoing calls to other units and is not explicitly listed as being called by other units in the cross-reference table. However, the source code reveals its integration pattern:

*   **Creation Context**: Instances of `ProcTriggeredData` are created within the `Unit` class methods (such as `Unit::ProcDamageAndSpellFor` or `Unit::IsTriggeredAtSpellProcEvent`, though these specific implementations are not in this file, the usage pattern is evident from the `ProcTriggeredList` typedef and `Unit::HandleTriggers` signature). The `Unit` class constructs these structs to capture the state of a potential proc.
*   **Consumption Context**: The `Unit` class consumes these structs via the `Unit::HandleTriggers` method, which accepts a `ProcTriggeredList const&`. This method iterates over the list of `ProcTriggeredData` instances to execute the actual spell triggers.
*   **Dependency**: The struct depends on the existence of `SpellProcEventEntry`, `SpellAuraHolder`, and `Unit` classes. It does not own these objects; it holds raw pointers, implying that the lifetime of the pointed-to objects must exceed the lifetime of the `ProcTriggeredData` instance during the processing of the proc list.

**Data Model**

This unit does not interact with any database tables. It operates entirely in memory using pointers to game objects and configuration entries loaded at runtime.

**Notable Implementation Details**

1.  **Aggregate Structure**: `ProcTriggeredData` is a simple aggregate with public members. This design allows for easy construction and access by the `Unit` class without the overhead of getter/setter methods.
2.  **Raw Pointers**: The struct uses raw pointers (`SpellProcEventEntry const*`, `SpellAuraHolder*`, `Unit*`). This indicates that `ProcTriggeredData` is intended for short-lived use within a single logical operation (processing a combat event). The caller must ensure that the `SpellAuraHolder` and `Unit` remain valid while the `ProcTriggeredList` is being processed. If a `Unit` dies or an aura expires during the processing of the list, dangling pointers could result, although the engine likely processes these lists atomically relative to major state changes.
3.  **Typedef Alias**: The struct is immediately followed by `typedef std::vector<ProcTriggeredData> ProcTriggeredList;`. This confirms its role as an element in a collection used to batch-process multiple potential procs generated by a single event.
4.  **No Copy/Move Semantics Defined**: As a simple aggregate with only pointer and integer members, the compiler-generated copy constructor and assignment operator perform shallow copies. This is appropriate for this use case, as the struct is typically copied into a vector or passed by value/reference, and the underlying objects are not owned by the struct.

## Member Reference

**ProcTriggeredData**
Constructor for the `ProcTriggeredData` struct. Initializes the `spellProcEvent`, `triggeredByHolder`, `target`, and `procFlag` members with the provided arguments. It does not perform any validation or memory management.

---

<!-- machine-true, projected from graph.json -->

## Map — ProcTriggeredData

*Source:* Unit.h

| Member | Kind | Calls out (other units) | Called by (other units) | Tables |
|---|---|---|---|---|
| ProcTriggeredData | ctor | — | — | — |
